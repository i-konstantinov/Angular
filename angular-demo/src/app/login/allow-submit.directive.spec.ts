import { AllowSubmitDirective } from './allow-submit.directive';

describe('AllowSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
